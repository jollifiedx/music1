[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Netlify's official Next.js runtime — handles App Router route handlers
# (our /api/generate and /api/status) as serverless functions.
[[plugins]]
  package = "@netlify/plugin-nextjs"
